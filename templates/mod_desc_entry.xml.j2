<!--
    FS25 modDesc.xml Entries
    Generated by FS25 Crop Maker

    Add the entries below to the appropriate sections of your modDesc.xml.
-->

<!-- 1) Inside <fruitTypes> ------------------------------------------------>
<fruitType filename="crops/{{ crop.name }}.xml"/>

<!-- 2) Inside <l10n> (localisation) --------------------------------------->
<text name="fruitName_{{ crop.name }}" en="{{ crop.title }}" de="{{ crop.title }}" fr="{{ crop.title }}"/>

<!-- 3) Inside <iconFiles> ------------------------------------------------->
<file name="icon_fruitType_{{ crop.name }}" filename="crops/textures/icon_fruitType_{{ crop.name }}.png"/>
